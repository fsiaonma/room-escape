<!--index.wxml-->
<view class="container" data-weui-theme="dark">
  <!-- 用户 openid -->
  <view class="userinfo">
    <button 
      open-type="getUserInfo" 
      bindgetuserinfo="onGetUserInfo"
      class="userinfo-avatar"
      style="background-image: url({{avatarUrl}})"
      size="default"
    ></button>
    <view class="userinfo-nickname-wrapper">
      <button class="userinfo-nickname">{{userInfo.nickName}}</button>
    </view>
  </view>

  <!-- 车队列表 -->
  <view class="team-list">
    <view wx:for="{{teamList}}" wx:key="index" class="team-item">
      <view>{{item.shop_name}}</view>
      <view>{{item.topic_name}}</view>
      <view>{{item.date}}</view>
    </view>
  </view>

  <!-- 发车按钮 -->
  <button type="primary" bindtap="onSaveClick">发起车队</button>
</view>
